1. 'install_docker.yml' -- установка и запуск docker (вообще, что в случае mysql, что в случае с docker, установочные пакеты собраны таким образом, что после установки сервисы сами стартуют).
2. 'install_mysql.yml' -- установка mysql с последующим рестартом сервиса и выводом на экран состояния сервиса.